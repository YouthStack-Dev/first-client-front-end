name: BUID AND PUSH TO DOCKER HUB

on:
 push:
  branches:
    - chethanr

jobs:
  build-and-push-to-docker:
    runs-on: unbuntu-latest

    steps:
      - name: echo the  state ment
        run: echo "check on "
        